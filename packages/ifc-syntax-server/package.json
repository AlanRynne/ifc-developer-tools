{
  "name": "@alanrynne/ifc-syntax-server",
  "author": "Alan Rynne",
  "version": "1.0.0",
  "description": "Language server for IFC files.",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "license": "MIT",
  "dependencies": {
    "@alanrynne/ifc-syntax-ast-parser": "^0.1.2",
    "@alanrynne/ifc-syntax-parser": "^1.0.0",
    "vscode-languageserver": "^5.2.1"
  },
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "rm -rf ./dist && rm tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
